<?xml version="1.0" encoding="UTF-8"?>
<beans
	xmlns="http://www.springframework.org/schema/beans"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:int="http://www.springframework.org/schema/integration"
	xmlns:int-ip="http://www.springframework.org/schema/integration/ip"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/integration/ip http://www.springframework.org/schema/integration/ip/spring-integration-ip.xsd"
>

	<int-ip:tcp-connection-factory
		id="validationNodeServerConnectionFactory"
		type="server"
		host="${tcp-validation-host}"
		port="${tcp-validation-port}" />

	<int-ip:tcp-inbound-gateway
		connection-factory="validationNodeServerConnectionFactory"
		request-channel="serverBytes2StringChannel"
		error-channel="errorChannel" />

	<int:channel id="validationNodeIncomingTransfersChannel" />

	<int:service-activator
		input-channel="validationNodeIncomingTransfersChannel"
		ref="networkServer"
		method="process" />

	<int:object-to-string-transformer
		id="serverBytes2String"
		input-channel="serverBytes2StringChannel"
		output-channel="toNetworkNodeServer" />

	<int:transformer
		id="errorHandler"
		input-channel="errorChannel"
		expression="payload.failedMessage.payload + ':' + payload.cause.message" />
 
</beans>